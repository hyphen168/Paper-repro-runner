# 代码质量与安全评审（专家组 A · reviewer 输出汇总）

> 由主管依据子代理完成通知（源码行号静态取证）汇总落盘。子代理环境无写盘工具，全文见运行记录。

## 结论：P0 无；P1 8 项；P2 12 项

### P1（应修）
1. 后台流水线线程拿不到密码（app.py 提交后 task["password"] 仅存局部，线程重读 DB 无密码）→ 仅密码认证任务会失败；建议经 _get_exec_state 内存快照传递。
2. auto/tune 自动命令 `'"${PAPER_REPRO_AUTO_RUN_COMMAND}"'` 被整体引号包裹成单个词，bash -c 必然无法执行。
3. 粘贴私钥全文落盘为文件（密钥以明文形式写本机磁盘）。
4. start_app 以 0.0.0.0 无鉴权暴露（局域网可访问整个控制端）。
5. 取消/重提与单线程执行器存在生命周期竞态（cancel 只改 DB、旧线程仍写终态、提交忽略 started=False）。
6. e2e_task.py 文档串明文泄漏真实云密码（仅开发脚本）。
7-8. 见原输出（截断项）。

### P2（12 项，节选）
- 行长/未用变量可清理；仓库文档与实现主题描述不一致；若干命令注入测试缺失。

### 做得好
- 领域层零 streamlit、数据归家目录、bash 落盘执行、后台异常兜底完整、版本锁定。
